---
import Layout from '../layouts/Layout.astro'

const agents = [
  { name: 'Market Sizing', desc: 'TAM, SAM, SOM with bottom-up and top-down modeling across 200+ industry databases', icon: '◎', hex: '#e5484d' },
  { name: 'Competitor Intel', desc: 'Internet-scale crawling of competitors — funding, hiring, product launches, positioning maps, moat analysis', icon: '◈', hex: '#ffb224' },
  { name: 'Business Model', desc: 'Revenue model stress-testing, unit economics simulation, pricing elasticity analysis', icon: '◇', hex: '#46a758' },
  { name: 'Technical Feasibility', desc: 'Architecture complexity scoring, build-vs-buy analysis, infrastructure cost modeling', icon: '⬡', hex: '#00a2c7' },
  { name: 'Risk Assessment', desc: 'Multi-dimensional risk matrix — market, execution, regulatory, financial, competitive', icon: '△', hex: '#e5484d' },
  { name: 'GTM Strategy', desc: 'Channel analysis, CAC modeling, launch sequence planning, growth lever identification', icon: '▷', hex: '#ffb224' },
  { name: 'Financial Projections', desc: '3-year P&L modeling, runway scenarios, funding requirement analysis, sensitivity tables', icon: '▢', hex: '#46a758' },
  { name: 'Executive Summary', desc: 'Cross-agent synthesis with weighted confidence scoring and go/no-go recommendation', icon: '◉', hex: '#00a2c7' },
]

const swarmAgents = [
  { name: 'Market Sizing', hex: '#e5484d', pct: 92 },
  { name: 'Competitor Intel', hex: '#ffb224', pct: 87 },
  { name: 'Business Model', hex: '#46a758', pct: 78 },
  { name: 'Technical Feasibility', hex: '#00a2c7', pct: 71 },
  { name: 'Risk Assessment', hex: '#e5484d', pct: 65 },
  { name: 'GTM Strategy', hex: '#ffb224', pct: 58 },
  { name: 'Financial Projections', hex: '#46a758', pct: 44 },
  { name: 'Synthesis Engine', hex: '#00a2c7', pct: 31 },
]

const steps = [
  { num: '01', title: 'Describe what you want to build', desc: 'Tell us the idea in plain language — the problem, the customer, the vision. No pitch deck required. Our system decomposes it into researchable dimensions.', mono: 'INPUT' },
  { num: '02', title: 'An agent swarm runs internet-scale research', desc: 'Dozens of specialized agents fan out across the internet — crawling databases, analyzing competitors, modeling financials, stress-testing assumptions — all in parallel.', mono: 'SWARM' },
  { num: '03', title: 'Get your report in 30 minutes', desc: 'A synthesis engine cross-references every agent\'s findings, resolves conflicts, and scores confidence. Your report is delivered straight to your inbox — and available to download from the website anytime.', mono: 'DELIVER' },
]

const stats = [
  { value: '10x', label: 'more products launched per year than a decade ago — building is no longer the bottleneck', source: 'Y Combinator' },
  { value: '42%', label: 'of startups fail because they built something nobody needed', source: 'CB Insights' },
  { value: '$150K+', label: 'what a Big 4 firm charges for the research you\'ll get in 30 minutes', source: 'McKinsey / Deloitte' },
]

const plans = [
  {
    name: 'Free',
    price: '0',
    period: 'forever',
    desc: '10 validations every month, on us',
    features: ['10 validations/month', 'Market size analysis', 'Top 5 competitors', 'Basic risk score', 'PDF export'],
    cta: 'Get Started',
    featured: false,
  },
  {
    name: 'Pro',
    price: '299',
    period: 'one-time',
    desc: '10 deep research credits — use anytime',
    features: ['10 deep research credits', 'Full agent swarm analysis', 'Monte Carlo financials', 'GTM playbook', 'Investor-ready reports', 'Credits never expire'],
    cta: 'Buy Credits',
    featured: true,
  },
  {
    name: 'Lifetime',
    price: '599',
    period: 'one-time',
    desc: '10 deep researches/month — forever',
    features: ['10 deep researches/month', 'Everything in Pro', 'Lifetime access', 'Priority processing', 'White-label reports', 'Dedicated support'],
    cta: 'Go Lifetime',
    featured: false,
  },
]

const faqs = [
  { q: 'How is this different from asking ChatGPT?', a: 'ChatGPT gives you a single model\'s opinion. Bulwark deploys an entire swarm of specialized agents — each focused on one dimension of due diligence — that crawl the internet, cross-reference findings across thousands of sources, run financial simulations, and produce a structured report with confidence scores. It\'s the difference between asking a friend and hiring McKinsey.' },
  { q: 'What kind of research report do I get?', a: 'A 30+ page intelligence brief covering every dimension McKinsey would analyze: market sizing (TAM/SAM/SOM), competitor landscape, business model viability, technical feasibility, risk matrix, GTM strategy, financial projections with Monte Carlo simulations, and an executive summary with a weighted go/no-go recommendation. Synthesized from 1,000+ data sources. Delivered to your inbox in about 30 minutes — and always available to download from your dashboard.' },
  { q: 'What do you mean by "agent swarm"?', a: 'Instead of a single AI answering your question, Bulwark deploys dozens of specialized agents in parallel — each an expert in a specific domain. They independently research, then a synthesis engine cross-references their findings to resolve conflicts and produce a unified intelligence brief. It\'s how large consulting firms work, but at internet scale.' },
  { q: 'Who is this for?', a: 'Any founder deciding what to build next. Solo founders validating their first idea. Serial entrepreneurs choosing between concepts. Venture studios evaluating deal flow. Anyone who believes taste and judgment matter more than execution speed.' },
  { q: 'Is my idea kept confidential?', a: 'Yes. Your ideas are encrypted at rest and in transit. We never share, sell, or use your data to train models. You own your data and can delete it at any time.' },
]
---

<Layout title="Bulwark Research — AI-Powered Startup Idea Validation">

  <!-- ═══════════════════════════════════════════ -->
  <!-- NAVBAR                                      -->
  <!-- ═══════════════════════════════════════════ -->
  <nav class="fixed top-0 left-0 right-0 z-50 border-b border-white/5 bg-ink/80 backdrop-blur-xl">
    <div class="mx-auto max-w-6xl px-6 flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2.5 group">
        <!-- Shield + battlements mark -->
        <svg class="w-7 h-7" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path fill-rule="evenodd" d="M6 8V6H9V4h2v2h3V3.5L16 1.5l2 2V6h3V4h2v2h3v2c0 9-4 16-10 21C10 24 6 17 6 8zm3 1.5c0 7.5 3 13 7 16.5 4-3.5 7-9 7-16.5z" fill="currentColor"/>
          <path d="M16 9.5h7c0 7.5-3 13-7 16.5z" fill="var(--color-vermillion)"/>
        </svg>
        <!-- Name -->
        <span class="text-[14px] tracking-[0.06em] uppercase" style="font-family: 'Archivo Black', sans-serif;">Bulwark <span class="text-muted" style="font-family: 'Manrope', sans-serif; font-weight: 400;">Research</span></span>
      </a>

      <!-- Nav Links (desktop) -->
      <div class="hidden md:flex items-center gap-8 text-sm text-muted">
        <a href="#how-it-works" class="hover:text-paper transition-colors duration-200">How it works</a>
        <a href="#what-you-get" class="hover:text-paper transition-colors duration-200">What you get</a>
        <a href="/pricing" class="hover:text-paper transition-colors duration-200">Pricing</a>
        <a href="#faq" class="hover:text-paper transition-colors duration-200">FAQ</a>
      </div>

      <!-- CTA -->
      <div class="flex items-center gap-4">
        <a href="/pricing" class="hidden sm:inline-block text-sm text-muted hover:text-paper transition-colors">Sign in</a>
        <a href="#hero-input" class="btn-glow px-5 py-2 text-sm">Validate an Idea</a>
      </div>
    </div>
  </nav>


  <!-- ═══════════════════════════════════════════ -->
  <!-- HERO                                        -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="relative min-h-screen flex flex-col items-center justify-center px-6 pt-24 pb-16 overflow-hidden">
    <!-- Background effects -->
    <div class="hero-glow absolute inset-0 pointer-events-none"></div>
    <div class="bg-grid absolute inset-0 pointer-events-none opacity-40"></div>

    <!-- Floating background text -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 pointer-events-none select-none">
      <span class="font-serif text-[12vw] font-bold text-white/[0.015] whitespace-nowrap">
        JUDGMENT
      </span>
    </div>

    <!-- Content -->
    <div class="relative z-10 max-w-3xl mx-auto text-center">
      <!-- Overline -->
      <div class="overline-divider max-w-sm mx-auto mb-6 animate-fade-in" style="animation-delay: 0.1s;">
        The Last Human Moat
      </div>

      <!-- Headline -->
      <h1 class="section-title text-4xl sm:text-5xl md:text-6xl lg:text-7xl mb-6 animate-fade-up" style="animation-delay: 0.2s;">
        Everyone can build.<br/>
        Few know <span class="italic font-accent text-gradient-warm">what</span> to build.
      </h1>

      <!-- Subheadline -->
      <p class="text-lg md:text-xl text-muted max-w-xl mx-auto mb-10 animate-fade-up" style="animation-delay: 0.35s;">
        AI made execution cheap. Taste and judgment are the last moat.
        An agent swarm runs internet-scale research on your idea and delivers a Big 4-quality dossier to your inbox — in 30 minutes.
      </p>

      <!-- Input Area -->
      <div id="hero-input" class="animate-fade-up" style="animation-delay: 0.5s;">
        <div class="relative max-w-xl mx-auto">
          <div class="flex flex-col sm:flex-row gap-3 p-2 rounded-2xl border border-ink-border bg-ink-light/60 backdrop-blur-sm">
            <input
              type="text"
              placeholder="Describe your startup idea..."
              class="flex-1 bg-transparent px-5 py-3.5 text-paper placeholder-dim outline-none text-base rounded-xl"
            />
            <button class="btn-glow px-7 py-3.5 text-sm font-semibold whitespace-nowrap">
              Validate Now →
            </button>
          </div>
          <p class="mt-4 text-xs text-dim font-mono">
            First idea free · Delivered to your inbox in 30 min · No credit card
          </p>
        </div>
      </div>

      <!-- Trust Signals -->
      <div class="mt-16 animate-fade-up" style="animation-delay: 0.65s;">
        <p class="text-[10px] font-mono uppercase tracking-[0.2em] text-dim mb-5">Research infrastructure powered by</p>
        <div class="flex flex-wrap items-center justify-center gap-x-8 gap-y-4 text-dim">
          <!-- OpenAI -->
          <div class="flex items-center gap-2 opacity-50 hover:opacity-80 transition-opacity">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
              <path d="M22.282 9.821a5.985 5.985 0 0 0-.516-4.91 6.046 6.046 0 0 0-6.51-2.9A6.065 6.065 0 0 0 4.981 4.18a5.985 5.985 0 0 0-3.998 2.9 6.046 6.046 0 0 0 .743 7.097 5.98 5.98 0 0 0 .51 4.911 6.051 6.051 0 0 0 6.515 2.9A5.985 5.985 0 0 0 13.26 24a6.056 6.056 0 0 0 5.772-4.206 5.99 5.99 0 0 0 3.997-2.9 6.056 6.056 0 0 0-.747-7.073zM13.26 22.43a4.476 4.476 0 0 1-2.876-1.04l.141-.081 4.779-2.758a.795.795 0 0 0 .392-.681v-6.737l2.02 1.168a.071.071 0 0 1 .038.052v5.583a4.504 4.504 0 0 1-4.494 4.494zM3.6 18.304a4.47 4.47 0 0 1-.535-3.014l.142.085 4.783 2.759a.771.771 0 0 0 .78 0l5.843-3.369v2.332a.08.08 0 0 1-.033.062L9.74 19.95a4.5 4.5 0 0 1-6.14-1.646zM2.34 7.896a4.485 4.485 0 0 1 2.366-1.973V11.6a.766.766 0 0 0 .388.676l5.815 3.355-2.02 1.168a.076.076 0 0 1-.071 0l-4.83-2.786A4.504 4.504 0 0 1 2.34 7.872zm16.597 3.855l-5.833-3.387L15.119 7.2a.076.076 0 0 1 .071 0l4.83 2.791a4.494 4.494 0 0 1-.676 8.105v-5.678a.79.79 0 0 0-.407-.667zm2.01-3.023l-.141-.085-4.774-2.782a.776.776 0 0 0-.785 0L9.409 9.23V6.897a.066.066 0 0 1 .028-.061l4.83-2.787a4.5 4.5 0 0 1 6.68 4.66zm-12.64 4.135l-2.02-1.164a.08.08 0 0 1-.038-.057V6.075a4.5 4.5 0 0 1 7.375-3.453l-.142.08L8.704 5.46a.795.795 0 0 0-.393.681zm1.097-2.365l2.602-1.5 2.607 1.5v2.999l-2.597 1.5-2.607-1.5z"/>
            </svg>
            <span class="text-xs font-medium">OpenAI</span>
          </div>
          <!-- Anthropic -->
          <div class="flex items-center gap-2 opacity-50 hover:opacity-80 transition-opacity">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
              <path d="M13.827 3.52h3.603L24 20.48h-3.603l-6.57-16.96zm-7.258 0h3.767L16.906 20.48h-3.674l-1.343-3.461H5.017l-1.344 3.46H0L6.57 3.522zm1.21 5.662l-2.33 5.942h5.072L7.78 9.182z"/>
            </svg>
            <span class="text-xs font-medium">Anthropic</span>
          </div>
          <!-- Perplexity -->
          <div class="flex items-center gap-2 opacity-50 hover:opacity-80 transition-opacity">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm0 2.4a9.6 9.6 0 1 1 0 19.2 9.6 9.6 0 0 1 0-19.2zm-1.2 4.8v4.2H7.2L12 6v1.2zm2.4 0l4.8 5.4h-3.6v4.2L10.8 18v-1.2h1.2v-4.2h3.6l-4.8-5.4z"/>
            </svg>
            <span class="text-xs font-medium">Perplexity</span>
          </div>
          <!-- Firecrawl -->
          <div class="flex items-center gap-2 opacity-50 hover:opacity-80 transition-opacity">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 14.5v-5l-3 3 1.5-5.5L12 6l2.5 3-1.5 5.5-3-3v5z"/>
            </svg>
            <span class="text-xs font-medium">Firecrawl</span>
          </div>
          <!-- Snowflake -->
          <div class="flex items-center gap-2 opacity-50 hover:opacity-80 transition-opacity">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 1l1.5 3.5L17 3l-1.5 3.5L19 8l-3.5 1.5L17 13l-3.5-1.5L12 15l-1.5-3.5L7 13l1.5-3.5L5 8l3.5-1.5L7 3l3.5 1.5L12 1zm0 14l.9 2.1L15 16l-.9 2.1L16 20l-2.1-.9L13 22l-.9-2.1L10 20l2.1-.9L11 16l2.1.9L12 15z"/>
            </svg>
            <span class="text-xs font-medium">Snowflake</span>
          </div>
        </div>
        <div class="mt-6 flex items-center justify-center gap-2">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-emerald"></span>
          </span>
          <span class="text-xs text-dim font-mono">Early access · Limited spots</span>
        </div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-fade-in" style="animation-delay: 1s;">
      <div class="w-5 h-8 rounded-full border border-ink-border flex items-start justify-center p-1.5">
        <div class="w-1 h-2 rounded-full bg-muted animate-pulse"></div>
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- THE PROBLEM                                 -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="relative py-24 px-6">
    <div class="divider max-w-6xl mx-auto mb-24"></div>

    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16">
        <div class="overline-divider max-w-xs mx-auto mb-4 reveal">The Shift</div>
        <h2 class="section-title text-3xl sm:text-4xl md:text-5xl reveal delay-1">
          Building is easy now.<br/>
          <span class="italic font-accent text-gradient-warm">Deciding what to build is the hard part.</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {stats.map((stat, i) => (
          <div class={`card-glass rounded-2xl p-8 reveal delay-${i + 2}`}>
            <div class="font-serif text-4xl md:text-5xl font-bold text-gradient-warm mb-3 stat-num">
              {stat.value}
            </div>
            <p class="text-muted text-base leading-relaxed mb-4">{stat.label}</p>
            <span class="font-mono text-[0.625rem] uppercase tracking-widest text-dim">{stat.source}</span>
          </div>
        ))}
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- WHY BULWARK EXISTS                          -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="relative py-28 px-6 overflow-hidden">
    <!-- Background accent -->
    <div class="absolute inset-0 bg-grid opacity-40"></div>
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[400px] bg-radial-[ellipse_at_center] from-vermillion/5 to-transparent rounded-full blur-3xl"></div>

    <div class="relative max-w-4xl mx-auto">
      <!-- Overline -->
      <div class="text-center mb-8 reveal">
        <div class="overline-divider max-w-sm mx-auto">Why Bulwark Exists</div>
      </div>

      <!-- Manifesto -->
      <div class="space-y-8 reveal delay-1">
        <h2 class="section-title text-3xl sm:text-4xl md:text-5xl text-center text-gradient leading-tight">
          The world doesn't need another<br/>
          <span class="italic font-accent text-gradient-warm">AI chatbot wrapper.</span>
        </h2>

        <div class="max-w-2xl mx-auto space-y-6 text-lg text-muted leading-relaxed">
          <p class="reveal delay-2">
            Vibe coding made building trivially easy. Every founder can ship a product in a weekend. But the hard problem was never building — it was <span class="text-paper font-medium">knowing what to build</span>.
          </p>
          <p class="reveal delay-3">
            The best founders at the best companies have always had an unfair advantage: armies of analysts, Big 4 consultants, and proprietary market intelligence that cost hundreds of thousands of dollars. That's how they made better decisions — not better gut feelings, better <span class="text-paper font-medium">data</span>.
          </p>
          <p class="reveal delay-4">
            We built Bulwark to end that asymmetry. An autonomous agent swarm that runs the same internet-scale research a McKinsey engagement would — market sizing, competitor intelligence, financial modeling, risk analysis — and delivers it in minutes instead of months.
          </p>
          <p class="reveal delay-5">
            Because in a world where everyone can build, the founders who win are the ones with the <span class="text-paper font-medium">conviction</span> to know they're building the right thing.
          </p>
        </div>
      </div>

      <!-- Divider quote -->
      <div class="mt-16 pt-12 border-t border-ink-border reveal delay-6">
        <div class="flex flex-col md:flex-row items-center gap-8">
          <div class="flex-shrink-0">
            <svg class="w-14 h-14" viewBox="0 0 32 32" fill="none">
              <path fill-rule="evenodd" d="M6 8V6H9V4h2v2h3V3.5L16 1.5l2 2V6h3V4h2v2h3v2c0 9-4 16-10 21C10 24 6 17 6 8zm3 1.5c0 7.5 3 13 7 16.5 4-3.5 7-9 7-16.5z" fill="var(--color-vermillion)" opacity="0.3"/>
              <path d="M16 9.5h7c0 7.5-3 13-7 16.5z" fill="var(--color-vermillion)" opacity="0.5"/>
            </svg>
          </div>
          <blockquote class="text-xl md:text-2xl font-accent italic text-gradient-warm leading-relaxed">
            "We don't tell you what to build. We give you the intelligence to decide for yourself — with the same rigor a $150K consulting engagement would."
          </blockquote>
        </div>
      </div>
    </div>
  </section>

  <div class="divider max-w-6xl mx-auto"></div>

  <!-- ═══════════════════════════════════════════ -->
  <!-- HOW IT WORKS                                -->
  <!-- ═══════════════════════════════════════════ -->
  <section id="how-it-works" class="relative py-24 px-6">
    <div class="divider max-w-6xl mx-auto mb-24"></div>

    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-20">
        <div class="overline-divider max-w-xs mx-auto mb-4 reveal">How It Works</div>
        <h2 class="section-title text-3xl sm:text-4xl md:text-5xl reveal delay-1">
          Big 4 research quality.<br/>
          <span class="italic font-accent text-gradient-warm">30 minutes. Not 5 months.</span>
        </h2>
      </div>

      <!-- Steps -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-4">
        {steps.map((step, i) => (
          <div class={`relative reveal delay-${i + 2}`}>
            <!-- Connector line (desktop) -->
            {i < 2 && (
              <div class="hidden md:block absolute top-10 left-[calc(50%+40px)] w-[calc(100%-80px)] h-px bg-gradient-to-r from-ink-border to-ink-border/0"></div>
            )}

            <div class="text-center md:text-left">
              <!-- Step number -->
              <div class="inline-flex items-center gap-3 mb-6">
                <span class="font-mono text-xs text-dim tracking-widest">{step.mono}</span>
                <span class="w-8 h-px bg-ink-border"></span>
                <span class="font-serif text-5xl font-bold text-white/10">{step.num}</span>
              </div>

              <h3 class="font-serif text-xl font-semibold mb-3">{step.title}</h3>
              <p class="text-muted text-sm leading-relaxed max-w-xs mx-auto md:mx-0">{step.desc}</p>
            </div>
          </div>
        ))}
      </div>

      <!-- Process Visualization -->
      <div class="mt-20 reveal delay-4">
        <div class="card-glass rounded-2xl p-6 md:p-8 max-w-4xl mx-auto">
          <!-- Terminal header -->
          <div class="flex items-center gap-2 mb-2">
            <div class="w-3 h-3 rounded-full bg-vermillion/60"></div>
            <div class="w-3 h-3 rounded-full bg-amber/60"></div>
            <div class="w-3 h-3 rounded-full bg-emerald/60"></div>
            <span class="ml-3 font-mono text-xs text-dim">bulwark validate --deep --internet-scale</span>
          </div>

          <!-- Status bar -->
          <div class="flex items-center justify-between py-3 mb-4 border-b border-ink-border/50">
            <div class="flex items-center gap-4">
              <span class="font-mono text-[10px] text-emerald flex items-center gap-1.5">
                <span class="w-1.5 h-1.5 rounded-full bg-emerald animate-pulse"></span>
                SWARM ACTIVE
              </span>
              <span class="font-mono text-[10px] text-dim">47 agents deployed</span>
              <span class="font-mono text-[10px] text-dim">1,247 sources crawled</span>
              <span class="font-mono text-[10px] text-dim hidden md:inline">3.2M tokens processed</span>
            </div>
            <span class="font-mono text-[10px] text-muted">02:41 elapsed</span>
          </div>

          <!-- Agent swarm status -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-8 gap-y-2">
            {swarmAgents.map((agent, i) => (
              <div class="flex items-center gap-3 py-1.5">
                <div class="w-1.5 h-1.5 rounded-full animate-pulse-dot" style={`background:${agent.hex}; animation-delay:${i * 0.2}s`}></div>
                <span class="font-mono text-[11px] text-muted w-40 shrink-0">{agent.name}</span>
                <div class="flex-1 h-1 bg-ink-border rounded-full overflow-hidden">
                  <div
                    class="h-full rounded-full"
                    style={`background:${agent.hex}; width:${agent.pct}%`}
                  ></div>
                </div>
                <span class="font-mono text-[10px] text-dim w-10 text-right">{agent.pct}%</span>
              </div>
            ))}
          </div>

          <!-- Activity log -->
          <div class="mt-4 pt-4 border-t border-ink-border/50 space-y-1.5">
            <div class="font-mono text-[10px] text-dim opacity-70">
              <span class="text-emerald">✓</span> Market Sizing: Ingested 23 industry reports, 4 government databases
            </div>
            <div class="font-mono text-[10px] text-dim opacity-60">
              <span class="text-amber">→</span> Competitor Intel: Crawling 156 company profiles, analyzing funding rounds...
            </div>
            <div class="font-mono text-[10px] text-dim opacity-50">
              <span class="text-cyan">→</span> Financial Model: Running Monte Carlo simulation (10,000 iterations)...
            </div>
            <div class="font-mono text-[10px] text-dim opacity-40">
              <span class="text-dim">⋯</span> +39 more agents working across 847 data sources
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- WHAT YOU GET (Bento Grid)                   -->
  <!-- ═══════════════════════════════════════════ -->
  <section id="what-you-get" class="relative py-24 px-6">
    <div class="divider max-w-6xl mx-auto mb-24"></div>

    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16">
        <div class="overline-divider max-w-sm mx-auto mb-4 reveal">Your Intelligence Brief</div>
        <h2 class="section-title text-3xl sm:text-4xl md:text-5xl reveal delay-1">
          Every dimension<br/>
          <span class="italic font-accent text-gradient-warm">McKinsey would analyze</span>
        </h2>
        <p class="text-muted mt-4 max-w-lg mx-auto reveal delay-2">
          Enterprise-grade due diligence was gatekept by big companies with big budgets. Our agent swarm crawls the internet at scale to give every founder that same rigor.
        </p>
      </div>

      <!-- Bento Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {agents.map((agent, i) => (
          <div class={`card-glass rounded-2xl p-6 reveal delay-${(i % 4) + 2} ${i < 2 ? 'sm:col-span-1 lg:col-span-2' : ''}`}>
            <div class="flex items-start justify-between mb-4">
              <span class="text-2xl" style={`color:${agent.hex}`}>{agent.icon}</span>
              <span class="font-mono text-[10px] text-dim tracking-widest uppercase">Swarm Layer {String(i + 1).padStart(2, '0')}</span>
            </div>
            <h3 class="font-serif text-lg font-semibold mb-2">{agent.name}</h3>
            <p class="text-sm text-muted leading-relaxed">{agent.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- REPORT PREVIEW                              -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="relative py-24 px-6 overflow-hidden">
    <div class="divider max-w-6xl mx-auto mb-24"></div>

    <!-- Background glow -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-vermillion/5 rounded-full blur-[120px] pointer-events-none"></div>

    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
        <!-- Left: Copy -->
        <div>
          <div class="overline-divider max-w-xs mb-4 reveal">The Report</div>
          <h2 class="section-title text-3xl sm:text-4xl reveal delay-1">
            A research report<br/>
            <span class="italic font-accent text-gradient-warm">worthy of a boardroom</span>
          </h2>
          <p class="text-muted mt-4 leading-relaxed reveal delay-2">
            Not a ChatGPT summary. A structured, data-backed intelligence brief synthesized from thousands of data points across hundreds of sources — delivered to your inbox in 30 minutes. The kind Big 4 firms charge $150K+ for.
          </p>

          <ul class="mt-8 space-y-4">
            {[
              'Multi-agent synthesis with cross-referenced confidence scoring',
              'Internet-scale research across 1,000+ data sources per report',
              'Monte Carlo financial simulations, not napkin math',
              'Delivered to your inbox when ready — also downloadable from the website',
              'Comparison mode for stress-testing multiple ideas',
            ].map((item, i) => (
              <li class={`flex items-start gap-3 reveal delay-${i + 3}`}>
                <span class="mt-1 w-1.5 h-1.5 rounded-full bg-vermillion shrink-0"></span>
                <span class="text-sm text-ivory">{item}</span>
              </li>
            ))}
          </ul>

          <div class="mt-10 reveal delay-5">
            <a href="#hero-input" class="btn-glow inline-flex px-7 py-3 text-sm">
              Try It Free →
            </a>
          </div>
        </div>

        <!-- Right: Report Mockup -->
        <div class="reveal delay-3">
          <div class="report-glow rounded-2xl border border-ink-border bg-ink-light/80 p-6 md:p-8">
            <!-- Report header -->
            <div class="flex items-center justify-between mb-6">
              <div>
                <div class="font-mono text-[10px] text-dim tracking-widest uppercase mb-1">Bulwark Report · Confidential</div>
                <div class="font-serif text-lg font-semibold">AI-Powered Pet Insurance</div>
              </div>
              <div class="flex items-center gap-2">
                <div class="w-2 h-2 rounded-full bg-emerald"></div>
                <span class="font-mono text-xs text-emerald">78/100</span>
              </div>
            </div>

            <div class="h-px bg-ink-border mb-6"></div>

            <!-- Report sections preview -->
            <div class="space-y-4">
              <!-- Market Size -->
              <div class="p-4 rounded-xl bg-ink/50 border border-ink-border/50">
                <div class="flex items-center gap-2 mb-2">
                  <span class="text-vermillion text-sm">◎</span>
                  <span class="font-mono text-xs text-muted">Market Sizing</span>
                </div>
                <div class="grid grid-cols-3 gap-3">
                  <div>
                    <div class="font-serif text-xl font-bold">$4.2B</div>
                    <div class="font-mono text-[9px] text-dim uppercase">TAM</div>
                  </div>
                  <div>
                    <div class="font-serif text-xl font-bold">$890M</div>
                    <div class="font-mono text-[9px] text-dim uppercase">SAM</div>
                  </div>
                  <div>
                    <div class="font-serif text-xl font-bold">$67M</div>
                    <div class="font-mono text-[9px] text-dim uppercase">SOM</div>
                  </div>
                </div>
              </div>

              <!-- Competitor landscape -->
              <div class="p-4 rounded-xl bg-ink/50 border border-ink-border/50">
                <div class="flex items-center gap-2 mb-3">
                  <span class="text-amber text-sm">◈</span>
                  <span class="font-mono text-xs text-muted">Competitor Landscape</span>
                </div>
                <div class="space-y-2">
                  {['Lemonade Pet', 'Trupanion', 'Healthy Paws'].map((comp, i) => (
                    <div class="flex items-center gap-3">
                      <div class="w-5 h-5 rounded bg-ink-mid border border-ink-border flex items-center justify-center text-[8px] font-bold text-muted">
                        {comp[0]}
                      </div>
                      <span class="text-xs text-ivory flex-1">{comp}</span>
                      <div class="flex gap-0.5">
                        {[1,2,3,4,5].map((_, j) => (
                          <div class={`w-4 h-1.5 rounded-sm ${j <= 3 - i ? 'bg-amber/60' : 'bg-ink-border/40'}`}></div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Risk Score -->
              <div class="p-4 rounded-xl bg-ink/50 border border-ink-border/50">
                <div class="flex items-center gap-2 mb-2">
                  <span class="text-vermillion text-sm">△</span>
                  <span class="font-mono text-xs text-muted">Risk Assessment</span>
                </div>
                <div class="flex items-center gap-4">
                  {[
                    { label: 'Market', level: 'Low', hex: '#46a758' },
                    { label: 'Execution', level: 'Med', hex: '#ffb224' },
                    { label: 'Regulatory', level: 'High', hex: '#e5484d' },
                  ].map((risk) => (
                    <div class="flex items-center gap-2">
                      <div class="w-1.5 h-1.5 rounded-full" style={`background:${risk.hex}`}></div>
                      <span class="font-mono text-[10px] text-dim">{risk.label}: <span style={`color:${risk.hex}`}>{risk.level}</span></span>
                    </div>
                  ))}
                </div>
              </div>
            </div>

            <!-- Report footer -->
            <div class="mt-6 flex items-center justify-between">
              <span class="font-mono text-[9px] text-dim tracking-widest uppercase">Generated Feb 17, 2026 · 32 pages · 1,247 sources · 47 agents</span>
              <div class="flex gap-2">
                <div class="px-2 py-1 rounded text-[10px] font-mono text-dim border border-ink-border hover:border-muted cursor-pointer transition-colors">PDF</div>
                <div class="px-2 py-1 rounded text-[10px] font-mono text-dim border border-ink-border hover:border-muted cursor-pointer transition-colors">Share</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- SOCIAL PROOF MARQUEE                        -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="py-16 overflow-hidden border-y border-ink-border/50">
    <div class="flex animate-marquee whitespace-nowrap">
      {[...Array(2)].map(() => (
        <>
          {[
            '"Like having a McKinsey team on call for every new idea" — Sarah K., YC W24',
            '"Killed two bad ideas in a day. Saved us 6 months and $200K." — Marcus T., Serial Founder',
            '"We used to fly blind. Now we decide with data." — Priya R., Seed Stage',
            '"The research quality rivals what we got from Deloitte at 1/1000th the cost" — Jake M., Studio Lead',
            '"Taste is knowing what to build. Bulwark gives you the conviction." — Lisa C., Solo Founder',
          ].map((quote) => (
            <span class="mx-12 text-sm text-gradient-warm italic font-accent">{quote}</span>
          ))}
        </>
      ))}
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- PRICING                                     -->
  <!-- ═══════════════════════════════════════════ -->
  <section id="pricing" class="relative py-24 px-6">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16">
        <div class="overline-divider max-w-xs mx-auto mb-4 reveal">Pricing</div>
        <h2 class="section-title text-3xl sm:text-4xl md:text-5xl reveal delay-1">
          Big 4 rigor.<br/>
          <span class="italic font-accent text-gradient-warm">Founder-friendly pricing.</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        {plans.map((plan, i) => (
          <div class={`card-glass rounded-2xl p-8 reveal delay-${i + 2} ${plan.featured ? 'pricing-active' : ''} flex flex-col`}>
            {plan.featured && (
              <div class="mb-4">
                <span class="px-3 py-1 rounded-full bg-vermillion/10 border border-vermillion/20 font-mono text-[10px] text-vermillion uppercase tracking-widest">
                  Best Value
                </span>
              </div>
            )}

            <div class="mb-6">
              <h3 class="font-serif text-xl font-semibold mb-1">{plan.name}</h3>
              <p class="text-sm text-dim">{plan.desc}</p>
            </div>

            <div class="mb-8">
              <span class="font-serif text-4xl font-bold">
                {plan.price === '0' ? 'Free' : `$${plan.price}`}
              </span>
              {plan.price !== '0' && (
                <span class="text-sm text-dim ml-1">{plan.period}</span>
              )}
            </div>

            <ul class="space-y-3 mb-8 flex-1">
              {plan.features.map((feature) => (
                <li class="flex items-start gap-2.5 text-sm">
                  <svg class="w-4 h-4 text-emerald shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-ivory">{feature}</span>
                </li>
              ))}
            </ul>

            <button class={`w-full py-3 rounded-full text-sm font-semibold transition-all ${plan.featured ? 'btn-glow' : 'btn-outline'}`}>
              {plan.cta}
            </button>
          </div>
        ))}
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- FAQ                                         -->
  <!-- ═══════════════════════════════════════════ -->
  <section id="faq" class="relative py-24 px-6">
    <div class="divider max-w-6xl mx-auto mb-24"></div>

    <div class="max-w-2xl mx-auto">
      <div class="text-center mb-16">
        <div class="overline-divider max-w-xs mx-auto mb-4 reveal">FAQ</div>
        <h2 class="section-title text-3xl sm:text-4xl reveal delay-1">
          Common questions
        </h2>
      </div>

      <div class="space-y-4">
        {faqs.map((faq, i) => (
          <details class={`group card-glass rounded-xl reveal delay-${(i % 4) + 2}`}>
            <summary class="flex items-center justify-between cursor-pointer p-6 text-sm font-medium text-ivory hover:text-paper transition-colors list-none">
              {faq.q}
              <svg class="w-4 h-4 text-dim shrink-0 ml-4 transition-transform group-open:rotate-45" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
              </svg>
            </summary>
            <div class="px-6 pb-6 text-sm text-muted leading-relaxed">
              {faq.a}
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- FINAL CTA                                   -->
  <!-- ═══════════════════════════════════════════ -->
  <section class="relative py-32 px-6 overflow-hidden">
    <!-- Background -->
    <div class="absolute inset-0 bg-grid opacity-30 pointer-events-none"></div>
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[400px] bg-vermillion/5 rounded-full blur-[160px] pointer-events-none"></div>

    <div class="relative z-10 max-w-2xl mx-auto text-center">
      <div class="overline-divider max-w-md mx-auto mb-10 reveal">The Last Moat</div>
      <h2 class="section-title text-3xl sm:text-4xl md:text-5xl reveal delay-1">
        Taste decides what to build.<br/>
        <span class="italic font-accent text-gradient-warm">Bulwark gives you the proof.</span>
      </h2>
      <p class="text-muted mt-4 mb-10 reveal delay-2">
        Your first idea is free. Get a Big 4-quality dossier delivered to your inbox in 30 minutes — no credit card, no catch.
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 reveal delay-3">
        <a href="#hero-input" class="btn-glow px-8 py-3.5 text-sm">
          Validate Your Idea — Free
        </a>
        <a href="#" class="btn-outline px-8 py-3.5 text-sm">
          See Sample Report
        </a>
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════ -->
  <!-- FOOTER                                      -->
  <!-- ═══════════════════════════════════════════ -->
  <footer class="border-t border-ink-border py-16 px-6">
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-12">
        <!-- Brand -->
        <div class="col-span-2 md:col-span-1">
          <div class="flex items-center gap-2.5">
            <svg class="w-7 h-7" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path fill-rule="evenodd" d="M6 8V6H9V4h2v2h3V3.5L16 1.5l2 2V6h3V4h2v2h3v2c0 9-4 16-10 21C10 24 6 17 6 8zm3 1.5c0 7.5 3 13 7 16.5 4-3.5 7-9 7-16.5z" fill="currentColor"/>
              <path d="M16 9.5h7c0 7.5-3 13-7 16.5z" fill="var(--color-vermillion)"/>
            </svg>
            <span class="text-[14px] tracking-[0.06em] uppercase" style="font-family: 'Archivo Black', sans-serif;">Bulwark <span class="text-muted" style="font-family: 'Manrope', sans-serif; font-weight: 400;">Research</span></span>
          </div>
          <p class="mt-3 text-sm text-dim leading-relaxed max-w-xs">
            Big 4-quality due diligence for founders who know that judgment — not execution — is the real moat.
          </p>
        </div>

        <!-- Product -->
        <div>
          <h4 class="font-mono text-[10px] uppercase tracking-widest text-dim mb-4">Product</h4>
          <ul class="space-y-2.5 text-sm text-muted">
            <li><a href="#how-it-works" class="hover:text-paper transition-colors">How it works</a></li>
            <li><a href="#what-you-get" class="hover:text-paper transition-colors">What you get</a></li>
            <li><a href="/pricing" class="hover:text-paper transition-colors">Pricing</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Sample Report</a></li>
          </ul>
        </div>

        <!-- Company -->
        <div>
          <h4 class="font-mono text-[10px] uppercase tracking-widest text-dim mb-4">Company</h4>
          <ul class="space-y-2.5 text-sm text-muted">
            <li><a href="#" class="hover:text-paper transition-colors">About</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Blog</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Careers</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Contact</a></li>
          </ul>
        </div>

        <!-- Legal -->
        <div>
          <h4 class="font-mono text-[10px] uppercase tracking-widest text-dim mb-4">Legal</h4>
          <ul class="space-y-2.5 text-sm text-muted">
            <li><a href="#" class="hover:text-paper transition-colors">Privacy</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Terms</a></li>
            <li><a href="#" class="hover:text-paper transition-colors">Security</a></li>
          </ul>
        </div>
      </div>

      <div class="h-px bg-ink-border mb-8"></div>

      <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
        <span class="text-xs text-dim">&copy; 2026 Bulwark Research. All rights reserved.</span>
        <div class="flex items-center gap-6">
          <a href="#" class="text-dim hover:text-paper transition-colors">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
          </a>
          <a href="#" class="text-dim hover:text-paper transition-colors">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
          </a>
          <a href="#" class="text-dim hover:text-paper transition-colors">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
          </a>
        </div>
      </div>
    </div>
  </footer>

</Layout>
